---
// Teams are not publicly browsable — users see only their own teams via /my-teams.
// Redirect to the user-scoped view, or to sign-in if not authenticated.
const user = Astro.locals.user;
if (!user) return Astro.redirect("/signin?redirect=/my-teams");
return Astro.redirect("/my-teams");
---
